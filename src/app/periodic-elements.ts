import { RPRowElement } from "./modules/rp-table/rp-row-element";

export class PeriodicElements implements RPRowElement {
  status: number;
  name: string;
  position: number;
  weight: number;
  symbol: string;
}
